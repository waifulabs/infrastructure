apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mmrelay-data
  namespace: matrix
  labels:
    app: mmrelay
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 1Gi