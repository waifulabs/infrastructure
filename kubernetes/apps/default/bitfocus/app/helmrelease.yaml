---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bitfocus
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: bitfocus
  values:
    controllers:
      bitfocus:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/bitfocus/companion/companion
              tag: v4.2.5@sha256:cec38556a763c4cc95a70b809594ad577ac8c0eeb3097ce27a3457f2998b1db8
            env:
              TZ: America/Los_Angeles
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
            #resources:
            #  requests:
            #    cpu: 100m
            #    memory: 64Mi
            #  limits:
            #    memory: 96Mi
        pod:
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            fsGroup: 1000
            fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        controller: bitfocus
        type: LoadBalancer
        ports:
          http:
            port: 8000
          satelite-api:
            port: 16622
          satelite-api2:
            port: 16623
          websocket:
            port: 28492
    route:
      app:
        hostnames: ["{{ .Release.Name }}.ok8.sh", "companion.ok8.sh"]
        parentRefs:
          - name: internal
            namespace: networking
            sectionName: https
    persistence:
      companion:
        existingClaim: bitfocus