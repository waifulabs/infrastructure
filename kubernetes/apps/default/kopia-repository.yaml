---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: kopia-repository-default
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  hostPath:
    path: /var/mnt/puddle/kubernetes/kopia
    type: Directory
  claimRef:
    namespace: default
    name: kopia-repository
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kopia-repository
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: ""
  volumeName: kopia-repository-default
