---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app dashboard
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: dashboard
  values:
    defaultPodOptions:
      imagePullSecrets:
        - name: ghcr-io-secret
    controllers:
      dashboard:
        containers:
          app:
            image:
              repository: ghcr.io/waifulabs/sunlight-dashboard
              tag: main@sha256:46180030647af4f590e29aa439d524b15af51803bf9e11f3de50de56e55bb2f2
            envFrom:
              - secretRef:
                  name: dashboard-secret
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: 3000
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
              startup:
                enabled: false
    service:
      app:
        ports:
          http:
            port: &port 3000
    route:
      app:
        hostnames: ["{{ .Release.Name }}.ok8.sh", "sunlight.ok8.sh"]
        parentRefs:
          - name: internal
            namespace: networking
            sectionName: https